{
  "name": "wehelper-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "wehelper Desktop tray shell",
  "author": "wehelper",
  "main": "src/main.cjs",
  "scripts": {
    "build:mac": "pnpm run prepare:bundle && electron-builder --mac dmg --publish never",
    "build:win": "pnpm run prepare:bundle && electron-builder --win msi --publish never",
    "dev": "electron .",
    "prepare:bundle": "node scripts/prepare-bundle.mjs"
  },
  "devDependencies": {
    "electron": "^32.3.3",
    "electron-builder": "^25.1.8"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron"
    ]
  },
  "build": {
    "appId": "com.wehelper.desktop",
    "productName": "wehelperDesktop",
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/**/*",
      "assets/**/*"
    ],
    "extraResources": [
      {
        "from": "runtime",
        "to": "runtime",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": ".bundle/wehelperBot",
        "to": "wehelperBot",
        "filter": [
          "**/*"
        ]
      }
    ],
    "mac": {
      "target": [
        "dmg"
      ],
      "artifactName": "wehelperDesktop-${version}-mac.dmg"
    },
    "win": {
      "icon": "assets/icon.ico",
      "target": [
        "msi"
      ],
      "artifactName": "wehelperDesktop-${version}-win.msi"
    }
  }
}
